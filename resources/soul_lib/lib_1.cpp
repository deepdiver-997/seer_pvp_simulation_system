#include <fsm/battleContext.h>

// 文件：soulmark_lib_1.c (编译成 lib_soulmark_1.so)
// 魂印效果函数1001
extern "C" void soulmark_effect_1001(BattleContext& context) {
    // 实现魂印1001的效果逻辑
    // 例如：启灵元神的神印效果
    // if (context.is_in_backpack(/*精灵ID*/)) {
    //     context.add_global_effect(/*某种全局效果*/);
    // }
}

// 魂印效果函数1002
extern "C" void soulmark_effect_1002(BattleContext& context) {
    // 实现魂印1002的效果逻辑
}
