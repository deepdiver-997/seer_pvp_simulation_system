# CLAUDE.md — 回合制精灵对战模拟引擎架构地图

回合制精灵对战模拟引擎（C++17，状态机驱动）。本文件是**精简公开版**架构地图：先看数据流建立整体感，再看各层职责。机制口径、效果实现与测试用例在内部文档中维护（未公开）。

## 0. 一句话

**数据（SQLite）→ 组装层（精灵/技能对象）→ 效果内核（注册表 + 时点桶）→ FSM 逐时点驱动 → 效果执行并读写 BattleContext。**

每次修改前先确认改的是哪一层：数据层、组装层（entities）、内核层（effects/、primitives/）、调度层（fsm/）、还是插件层。

## 1. 构建与运行

```bash
cmake -B build
cmake --build build -j8
```

目标：`sim_server`（对战服务器，127.0.0.1:4399）、`sim_smoke_test`（冒烟）等，见 CMakeLists。

## 2. 分层职责

| 层 | 位置 | 职责 |
|----|------|------|
| 数据层 | SQLite + `src/db/` | 只读查询仓库，固定接口不开放任意 SQL |
| 组装层 | `include/entities/` | 精灵、技能、印记对象：静态数据 + 效果分支注册 |
| 内核层 | `include/effects/` `include/primitives/` | 效果生命周期、规则中心（免疫/盔/命中失效）、伤害管线、原语 |
| 调度层 | `include/fsm/` `src/fsm/` | 有限状态机 + 时点桶：每个 State 是固定的效果注册时点 |
| 插件层 | 动态库（本地维护） | 效果实现，经 CoreApi 函数指针表与内核解耦，不链接内核符号 |

## 3. 设计原则

1. **机制是数据，不是 struct**：内核只提供少数固定注册表，新机制不改 FSM。
2. **时点是固定的语义节点**：效果只决定在哪个时点注册，不决定时点存在性。
3. **本体/视图分离**：能力等级等状态区分 pet 本体与回合内视图，公式读视图。
4. **插件与内核ABI解耦**：插件只经 CoreApi 调用，字段偏移一致性由构建系统保证。

## 4. 测试

场景化测试框架（每场景一个独立二进制）为**本地维护**，不入库；公开检出时 CMake 的 GLOB 为空、不影响构建。测试方法见 `docs/07-工具与测试/测试指南.md`。

## 5. 文档导航（公开部分）

- `docs/00-组件导航/` — 按目录的组件导航表
- `docs/01-架构与设计/` — 架构评审、演进、并发模型
- `docs/02-效果系统/效果系统内核设计.md` — 内核设计哲学
- `docs/07-工具与测试/` — 服务器协议与测试指南
- `PROVENANCE.md` — 与封存开发历史的延续关系说明

机制口径、效果实现细节、场景用例：见内部文档（未公开）。
